version: '3.8'

services:
  app:
    build:
      context: ./main-server
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    env_file:
      - .env
    restart: on-failure
[project]
name = "deploy-mcp"
version = "0.1.0"
description = "MCP server for remote deployment and observability"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=2.0.0,<3.0.0",
    "httpx>=0.27.0",
    "uvicorn>=0.30.0",
]

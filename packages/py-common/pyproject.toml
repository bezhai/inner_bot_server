[project]
name = "py-common"
version = "0.0.0"
description = "Shared Python utilities (demo)"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["py_common"]

